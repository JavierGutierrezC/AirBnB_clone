#!/usr/bin/python3

from models import base_model
import json

class FileStorage():

    __file_path = "file.json"
    __objects = []

    def all(self):
        return FileStorage.__objects

    def new(self,obj):
        __objects[self.__class__.__name__.id] = obj

    def save(self):
        if objects is None or objects is "":
            pass
        else:
            dict1 = []
            for key, value in FileStorage.__objects.items()
